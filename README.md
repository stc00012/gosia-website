# Gosia Website (ejemplo)

Sitio de ejemplo para desplegar en GitHub Pages con dominio personalizado `www.gosia.com`.

## Despliegue
1. Subir este repo a GitHub.
2. En Settings → Pages, seleccionar la rama `main` y la carpeta `/`.
3. Asegurarse que el DNS tiene un registro `CNAME` para `www` apuntando a `TU_USUARIO.github.io`